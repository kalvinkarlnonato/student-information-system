<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App.Views.Dialogs.ShowEnlistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid Width="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock Text="Student Enlistment Details" FontSize="20"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Student:"/>
                <TextBlock Text="{Binding Student.Person.FullName}"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Course:"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Student.Program}"/>
                    <TextBlock Text="-"/>
                    <TextBlock Text="{Binding Student.YearLevel}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Margin="10">
            <ListView HorizontalAlignment="Stretch" ItemsSource="{Binding StudentSubjects}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Code}" FontSize="12"/>
                            <TextBlock Text="{Binding Description}" FontSize="15"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Button Grid.Row="3" Margin="10" HorizontalAlignment="Right" Content="PRINT" Command="{Binding ShowCommand}"/>
        <Button Grid.Row="3" Margin="10" HorizontalAlignment="Left" Content="CANCEL" Click="Cancel_Click"/>
    </Grid>
</Page>
